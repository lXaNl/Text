// проверка массива на одинаковые елементы 
$arr=[1,3,1,1,2,2,4,4,4,4,4,4,4,4];
	foreach ($arr as $key => $elem) {
		foreach ($arr as $key2 => $elem2) {
			if ($elem==$elem2 and $key != $key2) {
				echo 'yes'."$key => $elem".'<br>';
				break ;
				//echo "$key2 => $elem2".'<br>';
			}
		}
	}
